<div>
  <h1 class="page-title"><%= @tournament.name %></h1>

  <div class="show">
    
    <h2>Players:</h2>
    <ul>
      <% @players.each do |player| %>
        <li><%= player.name %></li>
      <% end %>
    </ul>

    <h2>Games:</h2>
    <ul>
      <% @games.each do |game| %>
        <li><%= game.game_date %></li>
      <% end %>
    </ul>

    <!-- <table>
      <tr>
        <th>Blank</th>
        <% @players.each do |player| %>
          <th><%= player.name %>
        <% end %>
      </tr>
      <% counter=0 %>
      <% while true %>
        <% break if @players[counter] == nil %>
        <tr>
          <td><%= @players[counter].name %></td>
          <% counter += 1 %>
        </tr>
      <% end %>
    </table> -->

    <form method="POST" action="/players/<%= @tournament.id %>/delete">
      <button type="submit">Delete</button>
    </form>
  </div>
</div>